import React from 'react';
import ReactDOM from 'react-dom/client';
import QMPage from './QMPage';
ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><QMPage /></React.StrictMode>);
