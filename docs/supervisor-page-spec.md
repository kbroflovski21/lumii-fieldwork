# 服务主管页面 — 产品设计规格

状态：v2 设计定稿
日期：2026-05-15
仓库：`aro-network/lumii-fieldwork`
视觉规范：`docs/global-ui-guidance.md`

## 1. 页面定位

服务主管页面是一个**以 AI 对话为主要交互手段的文档管理界面**。

服务主管通过这个界面管理两类服务规范：

- **通用规范**：某一业务大类中，所有上门服务都必须遵循的流程性规则。例如"国家长期护理保险"要求所有上门服务人员必须自报家门、不得推销、必须询问满意度。这些要求与具体服务项目无关，是通用的流程约束。
- **服务项目规范**：实施某一项具体服务时，必须遵循的专业操作步骤。例如"清洁照护-口腔清洁"的 SOP 定义了从准备物品到记录口腔状况的 8 个操作步骤。

这两类规范作用在正交的两个维度上：通用规范是水平的流程层，服务项目规范是垂直的专业层。社工在执行一次上门服务时，需要同时遵守适用的通用规范和具体项目规范。

## 2. 规范的内部结构

每一个规范（无论通用还是服务项目）都包含固定的三个文件：

| 文件 | 性质 | 产生方式 |
|------|------|---------|
| **SOP** | 规章或标准定义的操作要求，是规范的源头 | 服务主管手动录入、上传文档、或口述后由 AI 整理 |
| **服务中实时督导要求** | 定义 AI 在社工上门服务过程中如何做语音督导 | AI 基于 SOP 自动推理生成。服务主管可审核和修改 |
| **服务后报告要求** | 定义服务结束后报告中需要提取和总结哪些信息 | AI 基于 SOP 自动推理生成。服务主管可审核和修改 |

**核心产品逻辑**：SOP 是唯一的源头。当 SOP 被创建或修改时，AI 自动理解 SOP 的要求，推理得出"服务过程中应该怎么督导"和"服务后报告应该包含什么"。服务主管不需要从零写督导和报告要求，只需要审核 AI 的推理结果并做微调。

## 3. 版本管理

每个规范文件夹有一个版本号。任何文件（SOP、督导要求、报告要求）的更新都会导致版本号 +1。

版本历史记录每次变更的：版本号、日期、变更摘要。

用户可以通过以下方式访问版本信息：
- 在 Tab 中，规范文件夹名称旁显示当前版本号（如 v3）
- 展开文件夹后，点击「v3 版本历史」按钮，在聊天框查看完整版本记录
- 在聊天中询问 AI，如"口腔清洁的版本历史是什么"、"对比 v2 和 v3 有什么变化"

## 4. 页面布局

### 整体结构

```
┌──────────────────────────────────────────────────────────┐
│ Header: [←] | [icon] 服务主管 / ● 规范管理工作台         │
├─────────────────────────────┬────────────────────────────┤
│                             │ [通用规范] [服务项目规范]    │
│  AI 对话区域                 ├────────────────────────────┤
│  （Qwen3-max 驱动）          │ 规范文件夹列表              │
│                             │ （展开/收起/CRUD）          │
│                             │                            │
│  [📎 上传] [输入框] [发送]   │ [+ 添加新规范]              │
└─────────────────────────────┴────────────────────────────┘
```

- 左侧：AI 对话区域，所有操作通过对话完成
- 右侧：Tab 面板（440px），展示规范文件夹结构，提供快捷操作入口
- Header：64px 高，紫色图标 + 标题 + 状态点

### 视觉规范引用

遵循 `global-ui-guidance.md` 定义的 token：
- 页面底色 `--page-bg`（#F7F9FB）
- 面板白底 `--surface`（#FFFFFF）
- AI 强调色 `--ai-accent`（#6366F1，紫色）——AI 头像、发送按钮、AI 生成标签
- 业务主色 `--accent`（#0052CC，蓝色）——Tab 选中态、加粗文本、编辑按钮
- 消息气泡 `--surface-subtle`（#F9FAFB）+ `--radius-chat`（左上 2px 其余 14px）
- 输入框 pill 形状（22px 圆角），发送按钮 40px 圆形紫色
- 状态标签使用语义色（success/warning/danger），6px 圆角

## 5. 对话区域

### AI 身份

- 角色名：规范管理助手
- 头像：32px 圆角方形，紫色渐变背景 + 白色 Bot 图标
- 每条 AI 消息都带头像

### 开场消息

用户首次进入时，AI 自动发送欢迎消息，列出当前系统中所有规范的名称和版本号，按通用规范和服务项目规范分组，并提示可用操作。

### 对话能力（由 LLM 驱动）

对话由 Qwen3-max 驱动，不是关键词匹配。AI 了解当前所有规范的状态（名称、版本、文件完成度、SOP 内容摘要），可以执行以下操作：

**查看**：
- 用户说「查看口腔清洁的 SOP」→ AI 在聊天中推送 SOP 全文
- 用户说「国家长护险的督导要求是什么」→ 推送督导要求全文
- 用户说「口腔清洁的版本历史」→ 推送版本列表

**新增规范**：
- 用户说「新增一个排泄照护-协助排泄的服务项目规范」
- 或用户点击 Tab 中的「+ 添加新规范」按钮（等同于用户发送"我想添加新的 XX 规范"）
- AI 回复提示用户提供 SOP 内容（口述或上传文件）
- 用户提供 SOP 后，AI 解析内容 → 自动生成 SOP + 督导要求 + 报告要求 → 预览并请求确认
- 用户确认后，规范创建，右侧 Tab 出现新文件夹

**编辑规范**：
- 用户说「我想编辑口腔清洁的规范」
- 或用户点击 Tab 中的「编辑」按钮（等同于用户发送"我想编辑 XXX 的规范"）
- AI 回复「您需要编辑什么？可以告诉我想调整哪个文件的什么部分，或者直接上传新文件给我。」
- 用户描述修改内容 → AI 给出修改预览 → 请求确认
- 用户确认后，文件更新，版本号 +1

**删除规范**：
- 用户说「我想删除口腔清洁的规范」
- 或用户点击 Tab 中的「删除」按钮（等同于用户发送"我想删除 XXX 的规范"）
- AI 回复确认提示，说明删除会移除该规范下的所有文件
- 聊天气泡中出现「确认删除」和「取消」按钮
- 用户点击确认后，规范从 Tab 中移除

**上传文档解析**：
- 输入框左侧有📎上传按钮，支持 .txt .md .doc .docx .pdf
- 用户上传文件后，文件内容发送给 LLM
- AI 解析文档内容，识别其中的规范要求，自动归类到对应的规范文件夹
- AI 回复解析结果：提取了什么信息、放在了哪个规范下、建议用户在 Tab 中查看
- 如果内容涉及新规范，AI 创建新文件夹；如果涉及已有规范，AI 更新对应文件

**AI 推理**：
- 新建或修改 SOP 后，AI 自动基于 SOP 推理生成「督导要求」和「报告要求」
- 三个文件在一次操作中同时创建/更新，用户无需分别处理
- 修改 SOP 后，AI 会同步更新督导和报告要求（或提示用户是否需要更新）

### 二次确认规则

所有涉及版本变更的操作（创建、更新、删除）都需要用户二次确认：

- 确认按钮嵌入在 AI 回复的消息气泡中
- 删除操作：红色「确认删除」+ 灰色「取消」
- 更新/创建操作：蓝色「确认」+ 灰色「取消」
- 按钮自动出现在包含确认提示关键词的 AI 消息下方
- 取消后 AI 回复"已取消"，不做任何修改

## 6. Tab 面板

### Tab 栏

两个 Tab：「通用规范」「服务项目规范」。选中态蓝色下划线 + 加粗，未选中灰色。

### 规范文件夹列表

每个规范显示为一个可展开的文件夹卡片：

**收起状态**：
- 📁 图标 + 规范名称 + 版本号标签（紫色背景，如 v3）
- 三文件状态：SOP ✓ | 督导 ✓ | 报告 ✓（绿色标签=完整，黄色=不完整，灰色横杠=空）
- 右侧展开箭头

**展开状态**：
- 📂 图标（打开态）
- 三个文件卡片（堆叠排列）：
  - 文件名 + 来源标签（手动录入 or 「AI 生成」紫色标签）
  - 内容预览（前两行，灰色小字）
  - 「查看完整内容 →」链接（点击在聊天框推送全文）
- 操作按钮行：
  - 「v3 版本历史」（紫色描边）——点击在聊天框推送版本记录
  - 「编辑」（蓝色描边）——点击等同于用户发送"我想编辑 XXX"
  - 「删除」（红色描边）——点击等同于用户发送"我想删除 XXX"

### 添加按钮

每个 Tab 列表最下方有「+ 添加新的 XX 规范」按钮（虚线框样式）。点击等同于用户发送"我想添加新的 XX 规范"，触发 AI 对话流程。

## 7. 文档结构（类 Git 文件夹）

```
通用规范/
  └── 国家长期护理保险/
        ├── SOP.md
        ├── 服务中实时督导要求.md
        └── 服务后报告要求.md

服务项目规范/
  ├── 清洁照护-口腔清洁/
  │     ├── SOP.md
  │     ├── 服务中实时督导要求.md
  │     └── 服务后报告要求.md
  ├── 基础健康观察-生命体征测量/
  │     └── ...
  └── ...（可通过 AI 对话动态添加更多项目）
```

服务项目命名格式：「服务组-项目名」，如「清洁照护-口腔清洁」。扁平二级结构，服务组不作为独立层级。

### 长护险服务项目完整清单（7 组 36 项）

| 服务组 | 项目 |
|--------|------|
| 进食饮水与营养支持 | 协助进食/水及指导、管饲饮食协助 |
| 排泄照护 | 协助排泄、失禁照护、人工取便、人工肛门/便袋清洁 |
| 清洁照护 | 洗发、理发、口腔清洁、指/趾甲清洁、擦浴、沐浴、更衣、整理床单位 |
| 体位与功能维护 | 体位管理、生活功能训练、翻身叩背排痰、压疮预防 |
| 基础健康观察 | 协助给药、生命体征测量、安全防护及指导 |
| 医疗护理 | 吸痰、氧气吸入、常规换药、雾化吸入、灌肠、置管、导尿、快速血糖测定、物理降温、造口护理、气管切开护理、深静脉置管护理 |
| 认知/运动/吞咽/言语功能训练 | 认知功能训练、运动功能训练、吞咽功能训练、言语功能训练 |

## 8. 数据模型

```typescript
interface StandardFolder {
  id: string;
  type: 'general' | 'service';
  name: string;
  version: number;
  sop: StandardDocument | null;
  supervision: StandardDocument | null;
  report: StandardDocument | null;
  history: VersionRecord[];
}

interface StandardDocument {
  status: 'complete' | 'incomplete' | 'empty';
  content: string;
  source: 'manual' | 'ai_generated';
}

interface VersionRecord {
  version: number;
  date: string;
  summary: string;
}
```

## 9. 与社工上门服务的关系

产品设计中，社工在执行一次上门服务时：

1. 系统加载该服务适用的**通用规范**的督导要求 + 报告要求
2. 系统加载该服务对应的**服务项目规范**的督导要求 + 报告要求
3. 两者合并后注入 LLM prompt，驱动实时语音督导和事后报告生成

当服务主管在本页面修改了任何规范，下一次社工执行相关服务时，督导行为和报告内容会自动反映最新的规范版本。

## 10. 技术实现要点

### LLM 对话端点

`POST /api/supervisor-chat`

- 输入：用户消息 + 当前所有规范文件夹状态 + 最近 10 条对话历史
- 系统 prompt 包含完整的角色定义、文档结构说明、可用操作列表和确认规则
- 强制 JSON 输出（`response_format: { type: 'json_object' }`）
- 输出：`{ reply: string, actions: Action[] }`
- Action 类型：`create_folder`、`update_doc`、`delete_folder`
- 前端接收到 actions 后立即更新本地文件夹状态

### 文件上传

- 输入框左侧📎按钮，触发 `<input type="file">`
- 客户端读取文件内容（`file.text()`），截取前 8000 字
- 作为用户消息发送给 LLM：「我上传了文件 XXX，内容如下：...」
- LLM 解析后在 actions 中返回创建/更新操作
