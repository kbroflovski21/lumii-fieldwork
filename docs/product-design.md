# 社区居家养老 AI 督导 H5 MVP 产品设计文档

状态：Superseded draft
日期：2026-05-11
仓库：`aro-network/lumii-fieldwork`
配套 superpowers spec：[`docs/superpowers/specs/2026-05-11-fieldwork-product-design.md`](superpowers/specs/2026-05-11-fieldwork-product-design.md)

> Superseded note: 2026-05-11 后，产品设计主线改为 [`docs/agentic-design-methodology.md`](agentic-design-methodology.md)、[`docs/business-use-cases.md`](business-use-cases.md)、[`docs/human-agent-substitution.md`](human-agent-substitution.md) 和 [`docs/agentic-use-case-analysis.md`](agentic-use-case-analysis.md)。本文保留为早期草稿和参考，不再作为 Agent 分类、业务主线或聊天通道设计的上游依据。

## 0. 参考源与防漂移约束

本产品设计文档明确 follow 并 reference 以下原始产品链接：

<http://124.221.48.52:3002/l/PlnMHgwtWwdW1zq7lAvUU2EQ/>

后续产品、设计、研发、测试、交付讨论如与本文发生冲突，应优先回到该链接核对原始意图；如参考链接内容更新，应在本文记录更新日期、变更点和决策人，避免口径漂移。

本文根据该链接在 2026-05-11 可访问内容整理，核心产品名为“社区居家养老 AI 督导 — H5 MVP 产品设计”，业务背景基于金色年华合作需求。

## 1. 产品定位

社区居家养老 AI 督导是一个面向居家养老上门服务场景的 H5 MVP。产品用手机 H5 + 蓝牙耳机先跑通完整业务闭环，未来可平滑迁移到硬件工牌，但不等待硬件。

这里的 MVP 不是演示原型，而是可以直接部署到现场试运行的最小生产系统。它必须覆盖从后台初始化、SOP 输入调试、排班派单、社工上门服务、AI 督导、日志/报告生成、管理审核到家属触达的全链路。

MVP 的核心目标是：

- 社工带手机和蓝牙耳机上门服务，AI 在服务过程中通过语音实时督导。
- 服务结束后，系统自动生成两份核心交付物：内部服务日志和家属健康报告。
- 站长可以排班、实时查看服务状态、通过 Agent 询问异常和服务质量。
- 家属可以在服务后通过飞书 channel 收到报告、查看历史趋势、给服务团队留言。

产品不是“表单录入工具”，也不是 dashboard-first 的后台系统，而是 Agent-first 的现场服务 cockpit：所有关键操作都由 Agent 主动发现、发起、解释和推进，web control panel / dashboard 只是承载当前 Agent 任务的结构化界面。

## 2. 设计原则

### 2.1 语音优先，服务中零操作

社工在服务过程中不应频繁看手机或操作表单。手机可以放在口袋里，蓝牙耳机承载主要交互；H5 屏幕只做状态监控、拍照、异常标记和必要兜底。

这一点必须与参考链接保持一致：H5 MVP 的交互模型要与未来工牌一致，载体不同，Agent 体验不变。

### 2.2 任务驱动，不是工具驱动

社工首页不是空白工作台，而是“今天跑哪几家”的任务队列。系统根据排班、路线、老人服务频次、异常优先级自动组织任务，社工按顺序到达、服务、离开。

### 2.3 Agent 感，不是表单感

系统通过 Agent 主动推进流程。Agent 是有主动性的业务 worker，不是菜单入口：

- 排班调度 Agent 主动发现服务需求、生成排班草案、分单、改派和推送确认卡。
- 服务督导 Agent 把 SOP 配置、SOP 调试和现场督导做成一体，因为 SOP 是现场督导的运行契约。
- 质检报告 Agent 在服务后主动生成内部日志、家属报告、缺证据提醒和飞书交付卡。
- Chat 是主交互形态；control panel / dashboard 是 Agent 给出的结构化状态、证据和确认面板。

### 2.4 两份日志是核心交付物

每次服务结束后必须自动生成：

- 服务日志：面向站长、机构、考核，包含 SOP 完成、质量评分、异常标记、照片、备注、转写记录。
- 健康报告：面向家属，包含服务摘要、健康指标、趋势、关注提醒，不能暴露内部质检数据。

所有功能设计都围绕这两份日志的自动生成与可信交付展开。

## 3. 用户角色

| 角色 | 使用端 | 核心诉求 | 核心能力 |
| --- | --- | --- | --- |
| 一线社工 | 社工端 H5 + 蓝牙耳机 | 少操作、按任务服务、避免遗漏 SOP | 今日任务、到达确认、实时语音督导、拍照、异常标记、服务归档 |
| 站长 / 管理层 | 管理端 H5 cockpit | 排班、看状态、控质量、看异常 | 排班调度、运营 panel、Agent 对话、日志审核、统计分析、人员与设备管理 |
| 老人家属 | 家属端 H5 | 知道服务做了什么、老人状态如何、有问题能提醒 | 服务报告、历史趋势、月度摘要、家属留言 |
| 总部管理员 | 管理端 H5 | 跨站点管理和经营分析 | 跨站数据、权限管理、模板配置、导出报表 |

### 3.1 现场部署角色分工

| 角色 | 现场部署职责 | 必须可用的生产能力 |
| --- | --- | --- |
| 总部管理员 | 初始化机构、站点、权限、跨站模板和全局配置 | 站点管理、角色权限、全局 SOP 模板、跨站数据查看、审计 |
| 站长 / 调度管理员 | 管理本站点每日运营 | 老人/家属/社工档案、设备绑定、SOP 调试、排班、异常处理、日志审核 |
| SOP 配置员 | 把服务类型转成可执行 SOP | SOP 新建/编辑/复制/停用、步骤排序、必问项、AI 提醒话术、预期时长、试跑调试 |
| 一线社工 | 接收派单并完成上门服务 | 今日任务、路线顺序、到达确认、语音督导、拍照、异常标记、完成服务 |
| 老人家属 | 接收服务结果并补充注意事项 | 飞书报告链接、历史趋势、留言、关注提醒 |
| AI Agent | 主动推进排班、服务督导和报告质检 | 排班草案、SOP 状态机、实时转写、异常检测、报告生成、脱敏、主动提醒 |

SOP 配置员可以由站长兼任，但产品上必须是独立能力。它不应拆成一个独立“配置 Agent”：现场上线前，服务督导 Agent 必须支持把“探访关爱”“助浴”等服务模板录入系统，试跑 AI 提醒话术，确认 SOP 步骤能驱动社工端服务流程。

### 3.2 从配置到上门到报告的完整现场闭环

1. 总部管理员创建机构、站点、角色权限和基础模板。
2. 站长录入社工、老人、家属、服务频次、地址、健康档案和飞书接收身份。
3. SOP 配置员创建或调整服务模板，配置步骤、必问项、AI 引导话术、预期时长和异常触发规则，并用模拟会话试跑。
4. 排班调度 Agent 主动生成次日排班草案和分单建议，站长确认后下发到社工端。
5. 社工打开 H5，看到今天被安排的上门服务任务、路线顺序和上次异常提示。
6. 社工到达老人家后确认到达，系统加载老人档案、SOP、历史记录和家属留言。
7. 服务过程中，AI 通过耳机实时督导，遗漏 SOP 步骤或发现风险时提醒；社工可以拍照和手动标记异常。
8. 服务结束后，系统生成内部服务日志、家属健康报告和社工 debrief。
9. 站长在管理端审核服务日志、异常标记和统计数据。
10. 家属通过飞书 channel 收到健康报告，查看趋势并留下注意事项，进入下一次服务上下文。

这条闭环必须在 MVP 中端到端可跑通，不能只交付某一端或某一个片段。

### 3.3 结果设计：Agent-first Cockpit

本产品是 agentic service，不是传统后台系统。MVP 的第一原则是：所有有业务意义的操作都应该从 Agent 的主动事件、建议、追问或确认卡开始；control panel / dashboard 只负责把 Agent 正在处理的业务对象结构化展示出来。

#### A. 交互骨架

- **Web 端**：Agent chat 可以浮在运营台、排班台、SOP 台、服务详情和报告台之上，也可以 dock 到右侧；用户在 chat 里表达意图、收到建议，在 panel 里看结构化证据、调参数、点确认。
- **飞书端**：看不到完整 panel/dashboard，所以 Agent 用消息 + 交互卡片承载压缩版 panel；复杂查看、批量调整、SOP 发布、日志审核通过 deep link 打开 web panel。
- **确认原则**：自然语言用于查询、分析、生成草案和解释；排班下发、SOP 发布、报告发送、日志归档等有后果动作必须通过卡片按钮或 panel 按钮确认。
- **范围原则**：MVP 不做泛 CRM，不做独立家属 chatbot，不做大型后台配置系统；只做让 Agent 能完成现场服务闭环的必要数据和面板。

#### B. Agent 角色分工

- **排班调度 Agent**：主动监控老人服务频次、待服务需求、社工可用性、路线、技能匹配、历史异常和临时变更；主动生成排班草案、分单、改派、顶替方案和飞书确认卡。主动分单归这个 Agent。
- **服务督导 Agent**：统一负责 SOP 模板录入/调试/发布和现场服务督导。SOP 不是单独后台功能，而是督导 Agent 的运行契约；同一个 Agent 要能在上线前试跑 SOP，也要能在社工上门时按 SOP 状态机提醒、追问、记录证据和标异常。
- **质检报告 Agent**：服务结束后读取转写、SOP 完成、照片、社工备注和历史档案，生成内部 ServiceLog、家属 HealthReport、缺证据提醒、脱敏检查和飞书交付卡。
- **家属沟通能力**：MVP 作为质检报告 Agent 的一个模式，不单独拆成第四个 Agent。它负责报告送达、家属留言收集和下次服务上下文回填。

用户角色是权限和受众，不是 Agent 拆分依据。站长会同时和排班调度 Agent、服务督导 Agent、质检报告 Agent 交互；社工主要和服务督导 Agent 交互；家属只接触质检报告 Agent 输出的报告和留言入口。

#### C. Control Panel / Dashboard 承载面

- **运营总览 panel**：今日概览、待确认、异常、超时、待审核、活动日志和 Agent 推荐动作，形态参考 dashboard repo 的 `/agent` 页面：<http://124.221.48.52:3000/agent>。
- **排班调度 panel**：展示 Agent 生成的排班草案、路线、任务卡、冲突原因、推荐社工、确认/调整/下发动作。
- **SOP/服务督导 panel**：展示 SOP 模板、步骤、提示话术、模拟调试记录、发布版本；服务中切换为 SOP 进度、转写摘要、证据、异常和实时提醒。
- **质检报告 panel**：展示 ServiceLog、HealthReport 脱敏预览、缺失证据、审核状态、飞书发送状态、失败重发。
- **基础配置 panel**：只保留 Agent 必须依赖的站点、社工、老人、家属、设备、飞书身份和服务频次数据。

#### D. 社工 H5 大界面

- **今日任务页**：显示路线顺序、老人姓名、地址、服务类型、预计时长、上次异常提示。
- **服务进行页**：展示老人、服务类型、计时、录音状态、SOP 进度、最近 AI 提醒、拍照、异常标记、完成服务。
- **服务完成页**：显示 debrief、AI 摘要、可补充备注、下一站；确认后自动归档并切换任务。

社工端不做复杂 chat。现场服务时主要通过耳机听 AI 提醒，屏幕只做状态和少量操作。

#### E. 家属/报告 H5 大界面

- **单次健康报告页**：服务摘要、健康指标、关注提醒、与上次对比、社工信息。
- **历史趋势页**：时间线和关键指标趋势。
- **留言页**：家属补充注意事项，进入下一次服务上下文。

家属端不暴露内部质检、原始转写、社工评分和管理备注。

## 4. 核心用户旅程

### 4.1 社工的一天

1. 上班打开 H5，看到按路线排列的今日任务列表。
2. 到达老人家后点击“到达”，系统加载老人档案、服务类型和 SOP；未来工牌可用 GPS 自动触发。
3. 服务过程中，社工正常与老人沟通，AI 在后台实时识别对话并按 SOP 节奏提醒。
4. 遇到伤口、药瓶、环境风险等情况，社工可拍照或手动标记异常。
5. 服务结束时，社工点击“完成”，或由 AI 识别告别语自动结束。
6. 系统生成服务日志、家属健康报告和社工 debrief，并切换到下一站。
7. 全部服务完成后，系统生成日终汇总，包括服务户数、SOP 完成情况和关注标记。

### 4.2 站长的一天

1. 排班调度 Agent 在前一天或当天早上主动发出“待确认排班草案”，说明老人需求、路线、社工能力、历史异常和冲突点。
2. 站长在飞书卡片或 web 排班 panel 中确认、调整或要求 Agent 重排；确认后任务下发到社工 H5。
3. 服务时段，站长主要看 Agent 主动推送的异常、超时、漏项、改派建议；运营 panel 负责展示当前状态和证据。
4. 站长可随时向浮层 Agent 提问，例如“今天有哪些异常”“小李本周表现怎么样”“王阿姨明天能不能换上午”。
5. 日终由质检报告 Agent 推送待审核日志和报告，站长在报告 panel 审核、退回补证据或确认发送。

### 4.3 家属服务后旅程

1. 每次服务结束后，家属通过飞书 channel 收到健康报告链接。
2. 打开 H5 后看到老人姓名、服务时间、服务类型、社工姓名、服务摘要。
3. 查看血压、用药、睡眠、情绪、行动力等指标，以及与上次服务的变化。
4. 如有异常，报告中突出关注提醒和建议。
5. 家属可查看历史服务时间线和关键指标趋势。
6. 家属可留言补充注意事项，该留言进入老人档案，并在下次服务时提醒社工。

## 5. MVP 功能范围

MVP 必须一次跑通三端全流程，不分期等待硬件。

### 5.1 社工端

- 今日任务队列：按路线排序，当前任务置顶，已完成任务收起。
- 到达确认：加载老人档案、服务类型、SOP 模板和历史关注点。
- 服务进行页：展示服务计时、录音状态、SOP 进度、最近一条 AI 提示、语音波形。
- 实时语音 AI 督导：WebSocket 上行音频，ASR 转写，LLM 判断 SOP 进度和异常，TTS 通过耳机提醒。
- 拍照记录：记录伤口、药瓶、环境等证据，关联服务会话。
- 手动异常标记：在 AI 未识别或社工主动判断时补充异常。
- 服务完成页：展示 AI 生成摘要，允许社工补充语音或文字备注。
- 自动归档：确认后生成服务日志、触发家属推送，并跳转到下一任务。

### 5.2 管理端

- 现场初始化：支持站点、角色、权限、社工、老人、家属、设备、飞书接收身份的基础配置。
- 排班调度：排班调度 Agent 主动生成按日排班草案，支持确认、调整、临时插入、改派、取消任务。
- 运营状态 panel：按社工、老人、时间线三种视图查看服务任务状态。
- 异常高亮：AI 标记异常的服务卡片应高亮，并展示摘要。
- Agent 对话：作为浮层或右侧 dock，支持查询异常、服务质量、社工表现、老人健康、排班草案和报告状态。
- SOP 模板管理：由服务督导 Agent 驱动，按服务类型维护步骤、AI 引导话术、预期时长、必做检查项；MVP 预置探访关爱和助浴等模板。
- SOP 调试：服务督导 Agent 支持用模拟老人档案和模拟对话试跑 SOP，查看 AI 是否按步骤提醒、是否误触发、是否能生成日志字段。
- 人员与组织管理：站点、社工、老人、家属 CRUD。
- 设备管理：手机设备 ID、蓝牙耳机型号、在线状态、最后活跃时间；未来支持切换到工牌。
- 服务日志浏览：支持按时间、社工、老人、异常状态筛选。
- 导出：支持 PDF / Excel，以满足机构和政府考核报表需求。
- 数据统计：服务量、SOP 完成率、异常率、社工效率、老人健康指标汇总。

### 5.3 家属端

- 服务报告页：移动端优先，一页式，大字体，高对比度。
- 健康指标卡片：血压、用药、睡眠、情绪、行动力等，以正常/关注/异常状态呈现。
- 服务内容摘要：用自然语言说明今天做了什么，避免内部质检话术。
- 关注提醒：异常或风险需要突出展示，并给出建议。
- 与上次对比：关键指标展示上升、下降、持平趋势。
- 历史记录：按时间倒序展示每次服务记录。
- 趋势图：血压、体重等关键指标支持时间范围切换。
- 月度健康摘要：自动汇总，为后续订阅付费能力打基础。
- 家属留言：简单文本输入，关联老人档案，进入下次服务上下文。

## 6. Agent 行为设计

### 6.1 排班调度 Agent

使用场景：站长日常运营、飞书排班卡、web 排班调度 panel。

输入上下文：

- 站点服务需求和老人服务频次
- 社工排班、可用时段、技能和服务能力
- 老人地址、路线、预计时长和历史异常
- 当前任务状态、请假、超时、临时插单
- 历史排班和完成质量

行为要求：

- 主动发现未排服务、排班冲突、社工空档、路线不合理和异常优先级变化。
- 主动生成次日/当日排班草案，给出分单理由、路线顺序、冲突说明和替代方案。
- 出现请假、超时、临时插单、服务中断时主动改派或重排，并通过飞书卡片或 web panel 请求确认。
- 排班下发、改派、取消必须经过站长确认；Agent 可生成草案但不能静默改变生产排班。

输出：

- 排班草案卡
- 推荐社工和路线理由
- 冲突/超时/改派提醒
- 任务下发确认卡
- 社工端任务队列更新

### 6.2 服务督导 Agent

使用场景：SOP 模板录入与调试、社工端服务过程中、服务异常追踪。

输入上下文：

- 当前老人档案
- SOP 模板
- 实时对话转写
- 历史服务记录
- 家属留言
- 当前服务状态和时间
- 模拟调试会话和 SOP 发布版本

行为要求：

- 支持站长/SOP 配置员录入、复制、停用、调试和发布 SOP 模板。
- 在上线前用模拟老人档案和模拟对话试跑 SOP，发现缺步骤、误提醒、日志字段缺失和话术问题。
- 以 SOP 状态机为主线，用 LLM 判断当前步骤是否完成、是否遗漏关键问题。
- 正常对话时保持安静，只在 SOP 节点、遗漏、异常、风险时介入。
- 能识别健康指标异常、情绪异常、安全风险和服务超时。
- 语气温和、专业、像经验丰富的督导，不打断老人和社工的自然交流。

输出：

- 耳机语音提示
- 服务中页面文字提示
- SOP 进度更新
- 异常标记
- 服务后 debrief
- SOP 调试报告和发布建议

### 6.3 质检报告 Agent

使用场景：服务结束后的日志和报告生成。

输入：

- 完整对话转写
- SOP 完成记录
- 照片
- 老人历史档案
- 社工备注
- 家属留言

输出：

- 服务日志：给管理层，结构化、可审计、可导出。
- 健康报告：给家属，自然语言、可读、脱敏。
- 社工 debrief：一句话总结服务结果和下一站提醒。
- 站长审核卡、缺证据提醒、飞书发送状态和失败重发建议。

脱敏规则：家属健康报告不能暴露内部质检评分、社工绩效评价、原始敏感对话全文和机构内部审核备注。

## 7. 技术架构

### 7.1 总体链路

三端 H5 通过 HTTPS / WebSocket 接入 API Gateway。后端服务层负责语音管道、Agent 引擎和业务服务。数据层使用 PostgreSQL、Redis 和对象存储。AI 能力优先接入阿里云百炼，接口层保留供应商切换能力。

```text
社工端 H5 / 管理端 H5 / 家属端 H5
        |
HTTPS / WebSocket
        |
API Gateway: 认证、路由、限流
        |
后端服务层
- 语音管道: ASR -> LLM -> TTS
- Agent 引擎: 排班调度、服务督导、质检报告
- 业务服务: 调度、管理、推送、导出
        |
数据层: PostgreSQL + Redis + OSS
        |
AI 服务: Paraformer / Qwen / CosyVoice
```

### 7.2 H5 技术选型

| 模块 | 选型 |
| --- | --- |
| 前端 | React，移动端优先响应式；如后续 repo baseline 已确定其他栈，再按 baseline 调整 |
| 音频采集 | Web Audio API + getUserMedia |
| 实时通信 | WebSocket，承载音频流和状态同步 |
| 后端 | Node.js 或 Python FastAPI |
| 数据库 | PostgreSQL |
| 实时状态 | Redis |
| 文件存储 | OSS，保存录音文件、照片、导出文件 |
| 推送 | 飞书 channel |

### 7.3 AI 模型选型

主方案 follow 参考链接中的阿里云全家桶方向：

| 环节 | 主选型 | 设计理由 |
| --- | --- | --- |
| ASR | 阿里 Paraformer v2 | 流式识别、说话人分离、热词定制，适合养老护理术语 |
| LLM | Qwen 系列 | 长上下文、Function Calling、Agent 框架和 SOP 状态机结合较成熟 |
| TTS | 阿里 CosyVoice | 低首包延迟，适合耳机实时提示 |
| 实时传输 | WebSocket | H5 音频上行和提示下发都可复用 |

备选方案：

- 火山引擎全家桶：延迟优先。
- 腾讯云 ASR + 智谱 GLM：方言覆盖和中文场景优先。

接口要求：ASR、LLM、TTS 必须通过内部抽象层接入，不能把供应商 SDK 直接散落到业务逻辑里。

## 8. 核心数据模型

| 实体 | 关键字段 |
| --- | --- |
| Site 站点 | 名称、区域、站长、社工列表 |
| Worker 社工 | 姓名、手机号、所属站点、设备 ID、负责老人 |
| Elder 老人 | 姓名、地址、健康档案、服务频次、默认 SOP 类型、注意事项 |
| Family 家属 | 姓名、手机号、与老人关系、飞书接收身份、推送渠道 |
| SOPTemplate SOP 模板 | 服务类型、步骤列表、AI 话术、检查项、预期时长、启用状态 |
| Schedule 排班 | 日期、社工、任务列表、路线顺序 |
| Session 服务会话 | 社工、老人、SOP、开始/结束时间、状态、SOP 进度、异常标记 |
| Transcript 对话记录 | 会话、时间戳、说话人、文本内容 |
| ServiceLog 服务日志 | 会话、SOP 完成情况、质量评分、异常、社工备注、照片 |
| HealthReport 健康报告 | 会话、老人、健康指标、摘要、关注事项、推送状态 |
| FamilyNote 家属留言 | 家属、老人、内容、创建时间、已读状态 |
| Recording 录音文件 | 会话、OSS 对象地址、时长、保留策略、访问审计 |

## 9. 权限与数据边界

| 角色 | 可见范围 | 可操作 |
| --- | --- | --- |
| 总部管理员 | 所有站点 | 全部功能、跨站分析、模板配置 |
| 站长 | 本站点 | 排班、看板、Agent 对话、日志审核、导出 |
| 社工 | 自己的任务和历史服务 | 执行服务、补充备注、拍照、标记异常 |
| 家属 | 关联老人的家属端报告 | 查看报告、趋势、留言 |

关键边界：

- 家属端只能看脱敏后的健康报告，不能看服务日志、原始转写、内部质检、社工评分。
- 社工只能查看自己的任务和相关老人必要档案，不能查看全站统计。
- 管理端导出和跨站查看需要权限审计。
- 录音、照片、健康信息属于敏感数据，必须有访问控制、操作日志和最小化展示。
- MVP 明确存储服务录音到 OSS，用于服务日志追溯、质量审核和争议处理；后续实现计划必须包含保留周期、访问审计和删除策略。

## 10. 验收标准

### 10.1 社工端验收

- 社工打开 H5 能看到当天任务队列，并按路线顺序执行。
- 到达后能加载老人档案、历史异常、SOP 模板。
- 服务过程中，AI 能根据对话推进 SOP，并在遗漏关键步骤时通过耳机提醒。
- 社工能拍照、手动标记异常、完成服务。
- 服务完成后系统能生成服务日志、健康报告、debrief，并跳到下一任务。

### 10.2 管理端验收

- 总部管理员能创建站点、角色、权限、社工、老人、家属、设备和飞书接收身份。
- 服务督导 Agent 能录入和调试探访关爱、助浴模板，并用模拟对话验证 AI 提醒和日志字段。
- 排班调度 Agent 能主动生成排班草案和分单建议，站长确认后同步到社工端。
- 运营 panel 能实时展示任务状态、异常、待确认和 Agent 推荐动作。
- Agent 能回答异常、社工表现、服务完成情况、排班草案和报告状态类问题。
- 服务日志可以筛选、查看、导出。
- 数据统计能按站点、社工、服务类型下钻。

### 10.3 家属端验收

- 服务完成后家属能收到推送并打开 H5 报告。
- 报告能展示服务摘要、健康指标、关注提醒和与上次对比。
- 历史记录和趋势图可用。
- 家属留言能进入老人档案，并在下次服务时进入督导 Agent 上下文。

### 10.4 安全与脱敏验收

- 家属端不出现内部质检评分、社工绩效、原始敏感对话全文。
- 不同角色访问越权接口时被拒绝。
- 导出、查看敏感数据、访问照片/录音都有审计日志。

### 10.5 现场部署验收

- 从空站点开始，管理员能完成站点、人员、老人、家属、设备、SOP、排班的初始化配置。
- 站长能把一个真实服务任务派给指定社工，社工端能收到并按路线展示。
- 社工能完成一次上门服务闭环：到达、服务中 AI 督导、拍照/异常、完成、debrief。
- 系统能生成内部服务日志、家属健康报告，并通过飞书 channel 发送报告链接。
- 站长能在日终看到服务完成率、异常、SOP 完成情况和待审核日志。

## 11. MVP 非目标

- 不等待硬件工牌，不把硬件作为 MVP 前置条件。
- 不做复杂家属社交互动，家属端仅报告、趋势、留言。
- 不做完整 CRM 或财务系统。
- 不在 MVP 中追求最低 AI 成本；优先保证实时性、准确性和服务闭环。
- 不把管理端做成传统报表系统，Agent chat 是主交互，运营状态 panel/control panel 是状态和确认承载面。

## 12. 主要风险

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 老人方言、噪声、多人说话导致 ASR 错误 | SOP 判断和异常识别不准 | 热词、说话人分离、低置信度兜底、人工补充备注 |
| AI 提醒过多打断服务 | 社工体验下降 | 节奏控制策略，只在 SOP 节点和异常时介入 |
| 家属报告暴露内部信息 | 隐私和信任风险 | 日志与报告分离生成，家属报告脱敏审查 |
| H5 后台录音受系统限制 | 实时督导中断 | 明确前台运行要求，服务中保持唤醒，后续工牌承接 |
| 站长不信任自动排班 | 采用阻力 | Agent 给建议和理由，站长确认后生效 |

## 13. 已确认的 MVP 决策

- 排班系统：先按金色年华没有现成排班系统处理，MVP 自建排班调度；后续如发现已有系统，再作为集成变更处理。
- 服务录音：MVP 存 OSS，不只做瞬时处理；实现计划必须覆盖保留周期、权限、审计和删除策略。
- 推送渠道：MVP 统一走飞书 channel。
- SOP 模板：MVP 至少包含探访关爱和助浴，其他模板进实现计划时再排优先级。
- 前端栈：实现计划默认按 React 规划；如后续 repo baseline 已确定其他栈，再按 baseline 调整。

## 14. 下一步流程

本文已对齐 superpowers spec。进入任何实现计划前，下一步必须先使用 `superpowers:writing-plans`，并在计划中引用本文、配套 spec 和原始参考链接。

## 15. 文档维护规则

1. 本文的产品范围、交互原则和 MVP 清单必须持续对齐参考源：<http://124.221.48.52:3002/l/PlnMHgwtWwdW1zq7lAvUU2EQ/>。
2. 如果参考源内容变化，更新本文时必须新增变更记录。
3. 如果研发实现需要偏离参考源，必须在 PR 或设计评审中说明原因，并更新本文。
4. 如果后续新增代码架构、接口协议、测试设计，应在 `docs/` 下补充对应文档并反向链接本文。
