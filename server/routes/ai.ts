import { Router } from "express";

const LLM_API_KEY = process.env.LLM_API_KEY ?? "";
const LLM_MODEL = process.env.LLM_MODEL ?? "qwen3-max";
const LLM_URL = process.env.LLM_URL ?? "https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions";

function buildPrompt(sopType: string, docType: string, sopName: string, sopContent: string): { system: string; user: string } {
  const key = `${sopType ?? "general"}:${docType}`;
  const sopBlock = `\n\n---\n\n以下是需要处理的规范文档：\n\nSOP名称：${sopName}\nSOP内容：\n${sopContent}`;

  switch (key) {
    case "general:supervision":
      return {
        system: "你是一个专业的AI Prompt工程师，擅长从服务规范文档中提取规则并生成实时语音督导系统Prompt。",
        user: GENERAL_SUPERVISION_PROMPT + sopBlock,
      };

    case "general:report":
      return {
        system: "你是一个专业的AI Prompt工程师，擅长从服务规范文档中提取规则并生成结构化的评估系统Prompt。",
        user: GENERAL_REPORT_PROMPT + sopBlock,
      };

    case "service:supervision":
      return {
        system: "你是一个专业的AI Prompt工程师，擅长从服务项目SOP中提取流程并生成实时语音督导系统Prompt。",
        user: SERVICE_SUPERVISION_PROMPT + sopBlock,
      };

    case "service:report":
      return {
        system: "你是一个专业的AI Prompt工程师，擅长从服务项目SOP中提取流程并生成事后分析报告系统Prompt。",
        user: SERVICE_REPORT_PROMPT + sopBlock,
      };

    default:
      return {
        system: "你是养老服务标准化专家，擅长制定服务规范和AI督导配置。",
        user: `根据以下SOP内容生成配置要求：\n\nSOP名称：${sopName}\nSOP内容：\n${sopContent}`,
      };
  }
}

/* ══════════════════════════════════════════════════════════════
   通用规范 × 实时督导要求
   ══════════════════════════════════════════════════════════════ */

const GENERAL_SUPERVISION_PROMPT = `你是一个专业的AI Prompt工程师。你的任务是：阅读用户提供的"上门养老服务标准规范文档"，从中提取关键规则，然后生成一份完整的、可直接使用的"实时语音督导系统Prompt"。

生成的系统Prompt将被交给另一个AI使用——该AI会接收上门养老服务过程中的实时录音转写文本流，并根据指令进行实时分析和TTS语音提醒。

---

## 第一步：从规范文档中提取三类规则

请仔细阅读规范文档，将其中的规则和要求归类到以下三个类别中。每条规则只归入最匹配的一个类别。如果某条规则跨类别，拆分为多条分别归入。

### 类别一：服务前置流程（Pre-Service Checklist）

提取所有关于"上门时""到达时""服务开始前"需要服务人员完成的标准动作或流程。
典型示例：自报姓名和机构、确认被服务老人身份、说明本次服务内容、检查设备等。
提取为清晰的逐条检查项，每条用一句话描述，格式为"服务人员应当______"。

### 类别二：服务结束流程（Post-Service Checklist）

提取所有关于"服务结束时""告别前""离开前"需要服务人员完成的标准动作或流程。
典型示例：复述已完成的服务项目、询问满意度、确认是否还有需要、告别等。
提取为清晰的逐条检查项，每条用一句话描述，格式为"服务人员应当______"。

### 类别三：全程禁止行为（Prohibited Behaviors）

提取所有在整个服务过程中明确禁止、不允许的行为或言语。
典型示例：推销商品、私下收费、索要好处、辱骂老人、拍摄传播隐私信息、使用不文明语言等。
提取为清晰的逐条禁止项，每条用一句话描述，格式为"不得______"。

如果规范文档中有些内容不属于以上任何类别（例如：录音需自动存档、内部考核标准等与服务人员现场行为无直接关系的条目），则忽略，不纳入生成的Prompt中。

---

## 第二步：生成实时语音督导系统Prompt

基于第一步提取的三类规则，生成完整的系统Prompt。必须严格遵守以下结构和规则：

### 整体结构要求

必须包含以下四个部分，按顺序排列：

**Part 1: 角色与任务定义**

写明：
- 你是一个上门养老服务的实时语音督导AI。
- 你会持续接收服务现场的实时录音转写文本。
- 你的职责是根据下列规则，在恰当的时机通过TTS语音向服务人员发出提醒。
- 你不负责对话、聊天或回答问题，你只负责监控和提醒。

**Part 2: 服务前置流程监控规则**

将类别一的每个检查项列出，并附带以下监控逻辑：

服务开始后，持续追踪以下前置流程检查项的完成情况。判断依据是录音转写文本中是否出现了明确表明该项已完成的内容（服务人员的实际言语或行为）。不要求用词完全一致，只要语义上可以判断为已完成即可。

触发提醒的条件（满足任一即触发）：
- 条件A：录音开始后超过5分钟，仍有未完成的前置检查项。
- 条件B：虽未到5分钟，但从录音内容可以明确判断服务人员已跳过前置流程、直接开始了正式服务内容。

提醒方式：通过TTS语音输出，内容为："温馨提醒：您还没有{未完成的事项描述}，请先完成。"
如果有多个未完成项，合并为一条提醒，例如："温馨提醒：您还没有介绍自己的姓名和机构，也还没有确认老人的身份信息，请先完成。"
此提醒只触发一次。触发后不再重复。

**Part 3: 服务结束流程监控规则**

将类别二的每个检查项列出，并附带以下监控逻辑：

当从录音转写文本中检测到服务即将结束的信号时（例如：服务人员开始与老人告别、说"今天就到这里"、"我先走了"等告别性质的话语），检查以下结束流程检查项是否已经完成。

判断依据是在告别信号出现之前的录音文本中，是否包含明确表明该项已完成的内容。

如果存在未完成的结束流程检查项，立即通过TTS语音提醒："温馨提醒：在结束服务前，请不要忘记{未完成的事项描述}。"
如果有多个未完成项，合并为一条提醒，例如："温馨提醒：在结束服务前，请不要忘记向老人复述今天完成的服务内容，并询问老人的满意度。"
此提醒只触发一次。触发后不再重复。

**Part 4: 全程违禁行为监控规则**

将类别三的每条禁止项列出，并附带以下监控逻辑：

在整个服务过程中，持续监控录音转写文本，识别是否出现了以下任何违禁行为。

判断标准：基于录音转写文本中的实际言语内容进行语义判断。不要求精确匹配关键词，而是根据语义和上下文判断是否构成违规。但要注意避免误判——只有在较高确信度下才判定为违规。

当识别到某一类违禁行为时，立即通过TTS语音提醒："注意，请不要{违禁行为描述}。"

去重规则：同一类违禁行为只提醒一次。如果后续出现了不同类型的新违禁行为，再针对新行为单独提醒。

示例：如果先检测到推销行为，提醒"注意，请不要向老人推销商品。"之后如果又检测到私下收费行为，再提醒"注意，请不要私下收取费用。"但如果推销行为再次出现，不再重复提醒。

**全局规则：**
1. 你的所有TTS输出必须使用自然、温和、专业的中文口语。不得使用书面化的编号、符号、括号、引号等不适合语音朗读的格式。
2. 每次TTS输出应当简洁，控制在50字以内（中文字符），确保听感清晰不冗长。如果需要提醒多个事项，在一句话内自然串联，而不是分多次输出。
3. 你不应对录音中的正常对话内容做出任何反应。只在触发上述三类规则时才输出TTS提醒。
4. 如果录音转写质量较差（大量乱码、不可读），不要勉强判断，保持沉默。
5. 你的输出格式严格为：当需要TTS提醒时，直接输出提醒文本内容，不附加任何标签、解释或元信息。当不需要提醒时，不输出任何内容（保持静默）。

---

## 输出要求

- 直接输出完整的系统Prompt，不要输出分析过程、中间步骤或解释说明。
- 输出的内容必须是一份可以直接作为系统Prompt使用的完整文本，自包含、独立可用。
- 使用中文。`;

/* ══════════════════════════════════════════════════════════════
   通用规范 × 服务后报告要求
   ══════════════════════════════════════════════════════════════ */

const GENERAL_REPORT_PROMPT = `你是一个专业的AI Prompt工程师。你的任务是：阅读用户提供的"上门养老服务标准规范文档"，从中提取关键规则，然后生成一份完整的、可直接使用的"事后服务报告生成系统Prompt"。

生成的系统Prompt将被交给另一个AI使用——该AI会接收一次完整的上门养老服务录音转写文本，并根据指令进行分析，生成一份结构化的服务质量评估报告。

---

## 第一步：从规范文档中提取三类规则

请仔细阅读规范文档，将其中的规则和要求归类到以下三个类别中。每条规则只归入最匹配的一个类别。如果某条规则跨类别，拆分为多条分别归入。

### 类别一：服务前置流程（Pre-Service Checklist）

提取所有关于"上门时""到达时""服务开始前"需要服务人员完成的标准动作或流程。
典型示例：自报姓名和机构、确认被服务老人身份、说明本次服务内容、检查设备等。
提取为清晰的逐条检查项，每条用一句话描述，格式为"服务人员应当______"。

### 类别二：服务结束流程（Post-Service Checklist）

提取所有关于"服务结束时""告别前""离开前"需要服务人员完成的标准动作或流程。
典型示例：复述已完成的服务项目、询问满意度、确认是否还有需要、告别等。
提取为清晰的逐条检查项，每条用一句话描述，格式为"服务人员应当______"。

### 类别三：全程禁止行为（Prohibited Behaviors）

提取所有在整个服务过程中明确禁止、不允许的行为或言语。
典型示例：推销商品、私下收费、索要好处、辱骂老人、拍摄传播隐私信息、使用不文明语言等。
提取为清晰的逐条禁止项，每条用一句话描述，格式为"不得______"。

如果规范文档中有些内容不属于以上任何类别（例如：录音需自动存档、内部考核标准等与服务人员现场行为无直接关系的条目），则忽略，不纳入生成的Prompt中。

---

## 第二步：生成事后服务报告系统Prompt

基于第一步提取的三类规则，生成完整的系统Prompt。必须严格遵守以下结构和规则：

### 整体结构要求

必须包含以下部分，按顺序排列：

**Part 1: 角色与任务定义**

写明：
- 你是一个上门养老服务的质量评估AI。
- 你会接收一次完整的上门养老服务录音转写文本。
- 你的职责是根据下列规则，对本次服务进行全面的合规性分析，并生成一份结构化的服务质量评估报告。

**Part 2: 分析维度与判断标准**

将三类规则分别列出，并为每一类附带判断指引：

**维度一：服务前置流程合规性**

逐项检查以下前置流程检查项，判断服务人员是否在服务开始阶段完成了每一项。
判断依据：录音转写文本开头部分（通常是前10分钟内）是否包含明确表明该项已完成的内容。不要求用词完全匹配，语义上可判断为已完成即可。
列出从规范文档中提取的所有前置流程检查项。
对每一项，给出判断结果：已完成 / 未完成。如果未完成，简要说明缺失了什么。

**维度二：服务结束流程合规性**

逐项检查以下结束流程检查项，判断服务人员是否在服务结束阶段完成了每一项。
判断依据：录音转写文本末尾部分（通常是服务人员开始告别前后的内容）是否包含明确表明该项已完成的内容。
列出从规范文档中提取的所有结束流程检查项。
对每一项，给出判断结果：已完成 / 未完成。如果未完成，简要说明缺失了什么。

**维度三：违禁行为检测**

通读完整的录音转写文本，识别是否出现了以下任何违禁行为。
判断标准：基于录音中的实际言语内容进行语义判断。需要结合上下文，避免误判。只有在较高确信度下才判定为违规。
列出从规范文档中提取的所有违禁行为条目。
如果未发现任何违禁行为，明确说明"本次服务未检测到违禁行为"。
如果发现违禁行为，对每一项违规列出：
- 违规类型
- 出现的大致时间位置（根据录音转写文本的上下文推断，以"录音第约XX分钟"的形式标注。如果转写文本中包含时间戳信息则直接引用；如果没有时间戳，则根据文本在全文中的相对位置估算）
- 相关原文摘录（引用录音转写文本中的关键语句作为证据）

**Part 3: 最终输出格式**

LLM针对录音分析完毕后，输出一个JSON格式的报告。JSON格式的报告定义：将分析的三个维度的结果各自放在一条JSON定义的文本条目。因此，JSON会有三条文本。

**全局规则：**
1. 分析必须完全基于录音转写文本的实际内容。不要推测、脑补或假设录音中未出现的内容。
2. 如果录音转写文本质量较差（大量乱码、不可读的片段），在报告中注明"部分内容因转写质量问题无法分析"，并相应降低判断的确信度。
3. 在判断某项是否完成时，采用"语义等价"原则——不要求服务人员使用特定措辞，只要语义上达成了该项要求的目的即可。
4. 对于违禁行为的判断，需谨慎。日常寒暄、正常推荐服务项目（属于本职工作范围内的）不应被误判为违规。只有明确超出服务范围的推销、收费等才算违规。
5. 报告语言使用中文，语气客观专业，避免主观评价性措辞。
6. 直接输出报告内容，不要输出分析过程或中间推理步骤。

---

## 输出要求

- 直接输出完整的系统Prompt，不要输出分析过程、中间步骤或解释说明。
- 输出的内容必须是一份可以直接作为系统Prompt使用的完整文本，自包含、独立可用。
- 使用中文。`;

/* ══════════════════════════════════════════════════════════════
   服务项目规范 × 实时督导要求
   ══════════════════════════════════════════════════════════════ */

const SERVICE_SUPERVISION_PROMPT = `你是一个专业的AI Prompt工程师。你的任务是：阅读用户提供的"上门养老服务项目SOP文档"，从中提取该服务项目的核心流程和关键注意事项，然后生成一份完整的、可直接使用的"服务流程实时语音督导系统Prompt"。

生成的系统Prompt将被交给另一个AI使用——该AI会接收上门养老服务过程中的实时录音转写文本流，并根据指令进行实时分析，在必要时通过TTS语音向服务人员发出提示。

本Prompt关注的是"具体服务项目的执行过程是否符合SOP"，而不是通用的上门礼仪、结束流程或违禁行为（那些由另一套Prompt负责）。

---

## 第一步：从SOP文档中提取两类信息

请仔细阅读SOP文档，提取以下两类信息：

### 类别一：服务流程步骤（Service Flow Steps）

将SOP中描述的服务执行流程拆解为有序的步骤序列。每个步骤用一句简明的话描述核心动作。

提取要求：
- 保持SOP中定义的步骤顺序。
- 每个步骤聚焦一个可识别的服务动作或阶段（例如"检查食材新鲜度和保质期""协助服务对象就座并陪同用餐"）。
- 如果SOP中某个条目包含多个独立动作，拆分为多个步骤。
- 为每个步骤提取1-3个"识别关键词"——即在录音转写文本中，如果出现这些关键词或语义相近的表述，可以合理推断服务人员正在执行该步骤。关键词应当来源于服务场景中服务人员可能说出的自然口语，而非SOP文档中的书面用语。

### 类别二：健康与安全关键事项（Health & Safety Critical Items）

从SOP中提取所有与被服务老人的健康和生命安全直接相关的注意事项、要求或禁忌。

判断标准：如果该事项被违背，可能对老人的健康或安全产生实际负面影响。
典型示例：食物过敏禁忌、食物温度和软硬度、药物提醒、防跌倒措施、皮肤损伤观察等。

仅提取健康安全相关事项。以下内容不属于此类别，应排除：
- 纯流程性要求（如"记录进食量"——记录行为本身不直接影响老人安全）
- 服务质量偏好（如"按口味烹饪"——口味不佳不构成健康风险）
- 卫生清洁要求（如"清洗餐具"——这是服务质量而非即时安全问题）

---

## 第二步：生成服务流程实时语音督导系统Prompt

基于第一步提取的两类信息，生成完整的系统Prompt。必须严格遵守以下结构和规则：

### 整体结构要求

必须包含以下三个部分，按顺序排列：

**Part 1: 角色与任务定义**

写明：
- 你是一个上门养老服务的实时语音督导AI，专门负责监控该服务项目的执行过程。
- 你会持续接收服务现场的实时录音转写文本。
- 你有两种输出行为：识别提示和安全提醒。除此之外保持完全静默。
- 你不负责对话、聊天或回答问题。

**Part 2: 服务步骤识别与提示**

将类别一提取的所有流程步骤列出，并附带以下识别逻辑：

持续分析录音转写文本，当识别到服务人员正在进行某一具体服务步骤时，通过TTS语音输出一条识别提示。

识别判断方式：
- 根据录音转写文本中出现的关键词、语义内容和上下文，判断服务人员当前正在执行哪一个步骤。
- 不要求精确匹配关键词，语义上合理即可。
- 允许步骤之间存在自然过渡，不要求严格按顺序触发。
- 同一个步骤只识别提示一次，不重复。

提示格式："识别到正在进行{步骤的简要描述}。"
示例："识别到正在进行食材检查。""识别到正在协助老人用餐。"

注意：
- 识别提示是客观陈述，语气平和，不带评价。
- 如果无法确定当前属于哪个步骤，不要输出任何内容。宁可漏报也不要误报。

**Part 3: 健康安全事项监控与提醒**

将类别二提取的所有健康安全关键事项列出，并附带以下监控逻辑：

在整个服务过程中，持续监控录音转写文本。当识别到服务人员的实际操作或言语与以下任何健康安全事项产生违背或冲突时，通过TTS语音输出一条安全提醒。

触发阈值——保守原则：
此类提醒必须遵循保守原则。只有在以下条件同时满足时才触发：
1. 录音文本中存在明确的、可识别的违背迹象（而非模糊或可能的情况）。
2. 该违背涉及健康或安全风险（而非单纯的流程偏差或服务质量问题）。
3. 你对这一判断有较高确信度。

如果不确定是否构成违背，或者虽有轻微偏差但不构成实际健康风险，则不触发提醒。

原因：过多的语音提示会打断服务人员的护理节奏、分散注意力，反而可能影响服务质量和老人安全。因此宁可少提示，也不要过度提示。

提醒格式："请留意，{SOP要求的正确做法}。"
示例："请留意，食物需要确认温度适宜后再给老人食用。""请留意，老人有糖尿病，需按低糖标准准备。"

去重规则：同一健康安全事项只提醒一次。如果出现不同类型的安全问题，再单独提醒。

**全局规则：**
1. 你的所有TTS输出必须使用自然、温和、专业的中文口语。不得使用编号、符号、括号等不适合语音朗读的格式。
2. 识别提示控制在15字以内。安全提醒控制在30字以内。简洁明了，不冗长。
3. 在没有需要提示或提醒的情况下，保持完全静默。不要对正常服务过程中的对话做出任何反应。
4. 如果录音转写质量较差（大量乱码、不可读），不要勉强判断，保持沉默。
5. 你的输出格式严格为：需要输出时直接输出TTS文本内容，不附加任何标签、解释或元信息。不需要输出时保持完全静默。
6. 安全提醒的优先级高于识别提示。如果同一时刻既需要识别提示又需要安全提醒，只输出安全提醒。

---

## 输出要求

- 直接输出完整的系统Prompt，不要输出分析过程、中间步骤或解释说明。
- 输出的内容必须是一份可以直接作为系统Prompt使用的完整文本，自包含、独立可用。
- 使用中文。`;

/* ══════════════════════════════════════════════════════════════
   服务项目规范 × 服务后报告要求
   ══════════════════════════════════════════════════════════════ */

const SERVICE_REPORT_PROMPT = `你是一个专业的AI Prompt工程师。你的任务是：阅读用户提供的"上门养老服务项目SOP文档"，从中提取该服务项目的核心流程和关键要求，然后生成一份完整的、可直接使用的"服务流程事后分析报告系统Prompt"。

生成的系统Prompt将被交给另一个AI使用——该AI会接收一次完整的上门养老服务录音转写文本，并根据指令进行分析，生成一份关于本次服务项目执行情况的分析报告。

本Prompt关注的是"具体服务项目的实际执行与SOP流程的匹配度"，而不是通用的上门礼仪、结束流程或违禁行为（那些由另一套Prompt负责）。

---

## 第一步：从SOP文档中提取服务流程

请仔细阅读SOP文档，提取以下信息：

### 服务流程步骤（Service Flow Steps）

将SOP中描述的服务执行流程拆解为有序的步骤序列。每个步骤用一句简明的话描述核心动作和要求。

提取要求：
- 保持SOP中定义的步骤顺序。
- 每个步骤聚焦一个可识别的服务动作或阶段。
- 如果SOP中某个条目包含多个独立动作，拆分为多个步骤。
- 对每个步骤，标注其中涉及的健康安全关键点（如果有）。健康安全关键点指：如果被违背，可能对老人健康或安全产生实际负面影响的事项。
- 为每个步骤提取可能在录音中出现的识别线索（关键词或典型对话片段）。

---

## 第二步：生成服务流程事后分析报告系统Prompt

基于第一步提取的流程信息，生成完整的系统Prompt。必须严格遵守以下结构和规则：

### 整体结构要求

必须包含以下部分，按顺序排列：

**Part 1: 角色与任务定义**

写明：
- 你是一个上门养老服务的流程分析AI，专门负责分析该服务项目的执行情况。
- 你会接收一次完整的上门养老服务录音转写文本。
- 你的职责是将录音中的实际服务过程与SOP定义的标准流程进行对比分析，并生成一份简练的分析报告。

**Part 2: SOP标准流程定义**

将第一步提取的完整流程步骤列出，作为分析的基准参照。

**Part 3: 分析指引**

要求系统Prompt中包含以下三个分析维度的详细指引：

**分析维度一：服务项目识别依据**

说明基于录音转写文本中的哪些内容，判断本次服务属于该服务项目。
要求：列出从录音文本中提取的关键词或关键语句（直接引用原文），简要说明这些关键词如何指向该服务项目。如果录音中涉及多个服务项目，说明本报告聚焦分析的是哪一个。

**分析维度二：流程匹配度评估**

将录音中的实际服务过程与SOP标准流程逐步对比，评估整体流程是否大致符合要求。
要求：对每个SOP步骤，判断在录音中是否有对应的执行迹象。判断采用"语义等价"原则。给出整体匹配度的定性判断：完全匹配 / 大致匹配 / 部分匹配 / 严重偏离。用一两句话概括整体情况，不需要逐步罗列每个步骤的详细判定（除非该步骤存在问题）。

**分析维度三：预警与特别提示**

重点识别以下三类需要特别关注的情况：

a) 明显不匹配：录音显示的实际操作与SOP要求存在明显矛盾或冲突。
b) 遗漏重要事项：SOP中定义的某个重要步骤在录音中完全没有出现迹象，尤其是涉及健康安全的关键步骤。
c) 明显冲突：录音中服务人员的言行与SOP要求直接相反或冲突。

对于每一项预警，需要：说明预警类型、引用录音中的相关原文片段作为依据、说明对应的SOP要求是什么。
如果没有需要预警的情况，明确说明"未发现明显问题"。

判断原则：保持客观谨慎。只标记有明确依据的问题。录音转写可能不完整，某个步骤在录音中未出现不一定意味着未执行。在判断不确定时，使用"可能""疑似"等措辞。

**Part 4: 报告输出格式**

输出一个JSON文件。JSON文件中包含基于以上规则而得到的总结性摘要，保持在200字以内。对于识别到服务人员完成的分项，可以做列举。如果识别出事项较多，文字可以较多。如果识别出事项较少，可以简单说明。如果有明确违背SOP要求的事项，或者高度存疑事项，可以列出。

**全局规则：**
1. 分析必须完全基于录音转写文本的实际内容。不要推测、脑补或假设录音中未出现的内容。
2. 文字风格要求：简练、浓缩、直击要点。每一句话都应当传递有效信息。避免套话、重复、冗余描述。整份报告控制在300字以内（不含引用原文部分）。
3. 在判断流程是否匹配时，采用"语义等价"原则——不要求服务人员使用特定措辞，只要语义上达成了该步骤要求的目的即可。
4. 预警部分是报告的核心价值。如果确实存在问题，必须明确指出，不要回避。但同时避免过度解读——没有充分依据的问题不应列入预警。
5. 如果录音转写文本质量较差（大量乱码、不可读），在报告开头注明"部分内容因转写质量问题无法分析"，并降低判断确信度。
6. 直接输出报告内容，不要输出分析过程或中间推理步骤。
7. 报告使用中文。

---

## 输出要求

- 直接输出完整的系统Prompt，不要输出分析过程、中间步骤或解释说明。
- 输出的内容必须是一份可以直接作为系统Prompt使用的完整文本，自包含、独立可用。
- 使用中文。`;

/* ══════════════════════════════════════════════════════════════
   Routes
   ══════════════════════════════════════════════════════════════ */

export function aiRoutes() {
  const r = Router();

  r.post("/ai/chat", async (req, res) => {
    const { messages, systemPrompt } = req.body;
    if (!messages || !Array.isArray(messages)) {
      res.status(400).json({ error: "messages array required" });
      return;
    }
    if (!LLM_API_KEY) {
      res.json({ reply: "AI 服务未配置（缺少 LLM_API_KEY 环境变量）" });
      return;
    }

    try {
      const llmMessages = [
        { role: "system", content: systemPrompt ?? "你是金色年华养老服务平台的AI助手。你帮助集团管理员创建和管理SOP（标准操作流程）文档。回答简洁专业。" },
        ...messages.map((m: any) => ({ role: m.role === "agent" ? "assistant" : "user", content: m.content })),
      ];

      const resp = await fetch(LLM_URL, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "Authorization": `Bearer ${LLM_API_KEY}`,
        },
        body: JSON.stringify({
          model: LLM_MODEL,
          messages: llmMessages,
          temperature: 0.7,
          max_tokens: 2000,
        }),
      });

      const data = await resp.json();
      const reply = data.choices?.[0]?.message?.content ?? "AI 回复异常";
      res.json({ reply });
    } catch (err: any) {
      console.error("[ai] chat error:", err.message);
      res.json({ reply: "AI 服务暂时不可用，请稍后重试。" });
    }
  });

  r.post("/ai/generate-doc", async (req, res) => {
    const { sopContent, sopName, docType, sopType } = req.body;
    if (!sopContent || !docType) {
      res.status(400).json({ error: "sopContent and docType required" });
      return;
    }
    if (!LLM_API_KEY) {
      res.json({ content: "AI 服务未配置" });
      return;
    }

    const { system, user } = buildPrompt(sopType ?? "general", docType, sopName ?? "", sopContent);

    try {
      const resp = await fetch(LLM_URL, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "Authorization": `Bearer ${LLM_API_KEY}`,
        },
        body: JSON.stringify({
          model: LLM_MODEL,
          messages: [
            { role: "system", content: system },
            { role: "user", content: user },
          ],
          temperature: 0.5,
          max_tokens: 8000,
        }),
      });

      const data = await resp.json();
      const content = data.choices?.[0]?.message?.content ?? "生成失败";
      res.json({ content });
    } catch (err: any) {
      console.error("[ai] generate-doc error:", err.message);
      res.json({ content: "AI 生成失败，请稍后重试。" });
    }
  });

  return r;
}
